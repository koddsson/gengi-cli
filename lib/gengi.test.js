import { expect } from 'chai'
import gengi from './gengi'

describe('Gengi utility', () => {
  it('should be a function', () => {
    expect(gengi).to.be.a('function')
  })

  xit('should output correctly formatted currency data')
})
